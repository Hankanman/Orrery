# Rust
/target
**/target
Cargo.lock.bak

# Node (docs + icon generation)
node_modules
.vite
*.local

# Release artifacts
/dist
/dist-appimage

# VitePress
docs/.vitepress/dist
docs/.vitepress/cache

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.swp

# Orrery runtime (should never live in repo)
*.sqlite
*.sqlite3
/models

# Bundled llama-server runtime — fetched by CI into packaging/llama-runtime/ for
# the AppImage build, never committed.
/packaging/llama-runtime/

# Cate IDE local workspace state (machine-specific layout)
.cate/
